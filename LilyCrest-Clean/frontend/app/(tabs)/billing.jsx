import BillingHistoryScreen from '../billing-history';

// Wrapper to reuse the full billing history UI inside the tab bar.
export default function BillingTab() {
  return <BillingHistoryScreen />;
}
